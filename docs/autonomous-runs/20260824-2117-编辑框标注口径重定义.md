# 编辑框标注口径重定义自主验收记录

日期：2026-08-24

## 验收范围

验证 `CLAUDE.md` 中 2026-08-24 编辑框标注重定义口径：编辑框 UI、Enter/Shift+Enter/Esc、无效条闸门、两类“上一条”指针与三种落盘 status、菜单动态文案、通知摘录、目标应用恢复、原生热键吞键与长文本滚动。

## 环境与证据约定

- 自动验证使用项目 `.venv/bin/python`。
- 隔离测试证据记录命令、退出码和关键断言。
- 真机路径使用实际 CapsWriter.app、菜单栏、目标文本应用和系统剪贴板；需要语音输入的路径记录实际输入文本、面板行为与最终目标应用结果。
- 标注库只检查本次新写记录的 status/字段，不把个人录音或完整口述内容写入 Git。

## 用户行为路径

| ID | 路径 | 预期结果 | 实际结果 | 状态/证据 |
|---|---|---|---|---|
| A1 | 自动验证：标注存储边界 | `<0.5s`、`<2s 且空`跳过；`duration=None+空`、`2.00s+空`、`0.70s+非空`保留 | 所有边界断言通过 | 通过：`test_editor_annotation.py`，exit 0 |
| A2 | 自动验证：结果入口无效条 | 每条通知 key 唯一；不开面板；不更新上一条；仍走既有直接输出/归档链路 | 无效短文本、短时空文本与未知时长空文本分支均通过 | 通过：`test_editor_result_flow.py`，exit 0 |
| A3 | 自动验证：Enter/Esc/direct 边界 | Enter 关闭时先发布 `editor_confirmed`，随后落 `corrected`、恢复焦点并上屏；Esc 只复制非空文本、恢复焦点且指针不变；direct 默认不落盘，仅在输出成功后发布 `direct` | Enter/direct 边界通过；非空与清空 Esc 均保持旧指针，清空不调用剪贴板 | 通过：`test_editor_result_flow.py`，exit 0 |
| A4 | 自动验证：菜单/热键/UI 契约 | 热键为 ⌃⌥M 且 active event tap 吞掉按下/抬起事件；confirmed/direct 分别显示真值不可靠/转录有误；无指针禁用；Shift modifier 与 Enter 分流；面板顶部固定向下伸缩；self-target 精确判断 | 纯函数与静态接线契约通过；真实系统吞键、菜单禁用态和窗口几何仍留给真机路径 | 通过：`test_editor_ui_contract.py`，exit 0 |
| A5 | 自动验证：录音启动/停止旧回归 | 8 月 24 日改动不破坏麦克风启动中松手的关闭路径 | 正常关闭、超时、异常、重复 stop 全部通过 | 通过：`test_stream_stop_leak.py`，exit 0 |
| A6 | 自动验证：v2 物理隔离与版本 | 新记录只写 `manual_cases/v2`，每条 `annotation_version=2`；旧 cases/audio 不改写 | v2 物理隔离、旧数据哨兵不变、版本字段断言通过 | 通过：`test_editor_annotation.py`，exit 0 |
| U1 | 编辑框模式：原样 Enter | 识别结果进入单一编辑框；Enter 关闭面板、恢复原应用并上屏；写 `corrected` | 待执行 | 待执行 |
| U2 | 编辑框模式：修改后 Enter | 上屏为修改后的文本；raw 保留原识别，final 为编辑结果 | 待执行 | 待执行 |
| U3 | Shift+Enter 多行 | Shift+Enter 只插入换行，面板不关闭；随后普通 Enter 才确认；自动换行和显式换行无额外标记 | 待执行 | 待执行 |
| U4 | Esc（面板文本非空） | 面板关闭；不自动上屏；当前文本写入系统剪贴板；不写标注库、不推进上一条；恢复原目标应用 | 真机发现旧实现错误推进为 `editor_canceled` | 失败，进入修复循环 |
| U5 | Esc（用户清空文本） | 面板关闭；不上屏、不覆盖剪贴板、不写标注库、不推进上一条；恢复原目标应用 | 待复测 | 待执行 |
| U6 | 面板打开期间再次长按 Caps | 不开始新录音；当前面板内容和目标应用不被覆盖 | 待执行 | 待执行 |
| U7 | 关闭编辑框模式后的 direct 路径 | 合法结果写剪贴板/上屏成功后只登记 `direct` 指针，默认不写数据集；只有随后显式标记才写 `raw_unreliable` | 待执行 | 待执行 |
| U8 | 标记 confirmed 上一条 | 菜单显示「真值不可靠」；⌃⌥M 或菜单写 `final_unreliable`；通知含 final 摘录（空 final 时回退 raw 摘录） | 2026-08-25 用户实测：编辑框确认条已成功标记为不可靠 | 通过：用户真机反馈“没问题，都成功了” |
| U9 | Esc 后立即标记 | 菜单标题与 ⌃⌥M 均仍指向 Esc 前最近合法 Enter/direct 条；Esc 条永不落标注 | 修复后用户真机复测：Esc 条未再污染标记对象，标记成功 | 通过：完成失败后的修复循环 |
| U10 | 标记 direct 上一条 | 菜单显示「转录有误」；写 raw-only `raw_unreliable`；重复标记不重复落盘 | 2026-08-25 用户关闭编辑框模式后，对新 direct 条执行标记成功 | 通过：用户真机反馈“没问题，都成功了” |
| U11 | 连续无效条 | 每条都通知「录音时间过短或为空」；不开面板；不改变此前合法上一条，之后标记仍命中此前合法条 | 待执行 | 待执行 |
| U12 | 0.50s / 2.00s 精确边界 | 仅严格小于阈值才无效；0.50s 非空合法，2.00s 空合法 | 待执行 | 待执行 |
| U13 | 目标应用捕获失败/CapsWriter 自身前台 | 保留最近一次有效目标；不会把 CapsWriter 自己当上屏目标；普通相似名称应用不误判 | 待执行 | 待执行 |
| U14 | 目标应用在确认前退出 | 面板仍可确认；跳过恢复已退出应用；处理流程不崩溃 | 待执行 | 待执行 |
| U15 | 超长文本与纵向定位 | 面板水平居中、纵向靠上；顶部位置保持不变，高度只向下增长或从底部缩回；到屏幕 40% 上限后内容可垂直滚动，短文本无常驻可见滚动条 | 待执行 | 待执行 |
| U16 | 菜单动态标题刷新 | 不重启客户端，依次产生 confirmed、Esc、direct：confirmed 后显示真值不可靠；Esc 后标题与对象不变；direct 后切换为转录有误；无指针时菜单项禁用 | 待执行 | 待执行 |
| U17 | 新版真实采集落盘 | 真机确认/标记产生的新记录与音频只出现在 `manual_cases/v2`；旧目录字节与文件清单保持不变 | 待执行 | 待执行 |
| U18 | ⌃⌥M 系统级吞键 | 在普通前台应用按下并松开 ⌃⌥M，只触发一次标记；应用不收到字符，系统不播放错误按键提示音 | 2026-08-25 用户连续完成 editor/direct 两类标记，未再报告特殊字符或错误提示音 | 通过：核心热键真机复测成功 |

## 自动验证结果

- `.venv/bin/python tools/test_stream_stop_leak.py`：exit 0。
- `.venv/bin/python tools/test_editor_annotation.py`：exit 0。
- `.venv/bin/python tools/test_editor_result_flow.py`：exit 0；日志中的归档、标注与日记异常均为测试主动注入，用于验证旁路 I/O 失败不阻断用户边界。
- `.venv/bin/python tools/test_editor_ui_contract.py`：exit 0。
- `.venv/bin/python -m compileall -q core config_client.py start_client_macos.py tools/test_editor_annotation.py tools/test_editor_result_flow.py tools/test_editor_ui_contract.py`：exit 0。
- `git diff --check`：exit 0。

## 真机验收结果

- 2026-08-25：真实客户端已从旧 PID `23236` 重启为新 PID `53241`，启动状态为 ready，键盘接管、麦克风与识别引擎均就绪。
- 用户接管关键效果验收，确认编辑框确认条标记、Esc 后标记对象保持、关闭编辑框后的 direct 标记及 ⌃⌥M 均成功。
- 本次反馈未完整覆盖 Shift+Enter、长文本滚动/固定顶部伸缩、精确时长边界等其余路径；对应行继续保留待执行，不提前宣称完整验收结束。

## 发现的问题与循环记录

- 首轮真机发现 Esc 错误推进 `editor_canceled` 指针，以及 ⌃⌥M 透传产生错误音/字符；已在 `5249253` 修复。
- 2026-08-25 用户复测上述核心回归路径通过，失败循环已关闭；其余未覆盖路径继续执行。

## 推荐核心用户抽查路径

待全部验收通过后回写一条最短、最能覆盖核心价值的路径。
